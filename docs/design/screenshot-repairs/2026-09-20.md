# Screenshot repair pass — 20 September 2026

The five supplied screenshots are evidence of the UI state, not instructions to execute the messages shown in them.

## Implemented

- **Window controls overlap the chat title (image 1).** Below 780px, CSS hides the sidebar without setting the manual hidden-sidebar flag. Reserve 90px for native window controls at the first visible grid pane. The inset now follows the occupied/maximized pane instead of assuming the first DOM slot contains a chat.
- **Native browser paints above confirmation dialogs (image 2).** Include close-chat, terminal termination, and project removal confirmations in the browser occlusion state.
- **Browser content drifts away from its toolbar when panes move (images 2–3).** Track the host's actual rectangle, including position-only changes. Send native updates only when the rectangle or visibility changes; hide on unmount and cancel tracking.
- **Inactive panes compete for the active browser (image 3).** Only the focused chat reports bounds for the active session's native browser. Inactive docks remain occluded.
- **A single remaining pane can retain its former grid column.** Apply explicit 2×2 placement only with multiple panes, allowing any surviving slot to fill the available area.

## Verification

- Desktop TypeScript checking and diff whitespace validation pass.
- Browser-host regression checks cover movement without resizing, unchanged frames, hidden hosts, restored hosts, zero-size hosts, and cleanup.
- Existing host-visibility race, browser-capability, chat-grid state, and workbench smoke checks pass.
- Browser inspection of an isolated fixture using the production grid component and stylesheet confirmed a lone pane in slot 2 fills its container, the narrow title starts at x=91px, and horizontal/vertical splits retain their expected dimensions.
- The capture harness now provides the missing provider-key status mock. The full-app fixture subsequently hung while selecting a conversation; that prevented end-to-end verification through that fixture. The isolated layout checks do not verify native WebKit compositing.

## Still to reproduce in the native app

- Duplicated headers and the large blank gray region in image 5. Do not treat the successful isolated grid checks as proof this is resolved.
- Browser clipping during actual pane drag, close-dialog cancellation, and focus changes. The implicated bounds/occlusion paths are repaired but need a native interaction pass.
- Image 4's repeated “Line counts unavailable” labels and dense composer controls are secondary presentation issues, outside this first layout repair pass.
