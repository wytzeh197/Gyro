# BYOK provider keys are stored in macOS Keychain by the CLI.
# Use environment variables only when importing keys:
#
# gyro config set-provider-key openai --env OPENAI_API_KEY
# gyro config set-provider-key anthropic --env ANTHROPIC_API_KEY

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
