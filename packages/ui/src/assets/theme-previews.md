# Theme preview artwork

The three theme-preview PNGs are cropped from the Appearance screenshot supplied by the user on 2026-09-12. They contain only the preview illustrations; labels and selection outlines are rendered by the app. Crops omit the reference's existing selection border so it is not baked into the Light option.

The Dark preview was recolored with the image-editing tool on 2026-09-12 at the user's request. Its foreground interface now uses charcoal matching the dark side of the System preview, with muted gray placeholder text.
